$(document).ready(function() {

    $(".ar-navbar").fadeIn(3000, "linear");

/*     var remainingTime = 2; 
    
    $('video').on('timeupdate', function() {
        var video = this;
        if (video.duration - video.currentTime <= remainingTime) {

        }
    }); */
});